# 2. Для списка реализовать обмен значений соседних элементов, т.е.
# Значениями обмениваются элементы с индексами 0 и 1, 2 и 3 и т.д.
# При нечетном количестве элементов последний сохранить на своем месте.
# Для заполнения списка элементов необходимо использовать функцию input().

my_list2 = list(input('Введите произвольный текст. Он может включать в себя любые символы, которые '
                 'Вы найдете на своей клавиатуре. Ни в чем себе не отказывайте :) '))
                 
# Я же правильно поняла, что содержание списка не принципиально в данном задании?
print(my_list2)
my_list2.insert(0, 1)
print(my_list2)
# (!) И вот тут у меня проблема. Почему-то результат выглядит так: 
# Введите произвольный текст. Он может включать в себя любые символы, которые Вы найдете на своей клавиатуре. Ни в чем себе не отказывайте :) 1234
# ['1', '2', '3', '4']
# [1, '1', '2', '3', '4']
# откуда новая единица в списке? 


# Создаю переменные, чтобы подставлять в insert значения индексов переменных, которые нужно менять местами
# first = 0
# second = first + 1
# Ввожу переменную, которая ограничит повторение операций концом списка
# end = len(my_list2)
# Повторяем изменение инекса и insert, пока список не кончится 
# while first < end:
#     my_list2.insert(first, second)
#     first = first + 2
#     second = second + 2
# print(my_list2)
